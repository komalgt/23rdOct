name: Deploy Workflow

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Example deploy step (replace with your actual command)
      - name: Deploy Service
        run: echo "Deploying application..."  # Replace with your deploy logic

    # This is just for example; you don't need 'outputs' unless you pass from a previous step
    outputs:
      status: ${{ steps.deploy-status.outputs.status }}  # Remove or adjust if unnecessary

  notify_status:
    needs: deploy
    uses: ./.github/workflows/notify-status.yml
    with:
      deployment_status: ${{ needs.deploy.result }}  # Or use a fixed value like "success" if you prefer
      repo: ${{ github.repository }}
      sha: ${{ github.sha }}
      recipient_email: "kturakannavar@deloitte.com"
