# Code Generated by Sidekick is for learning and experimentation purposes only.
name: Deploy and Notify

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    outputs:
      result: ${{ steps.deploy_step.outcome }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Dummy deploy step (replace with real deployment logic)
        id: deploy_step
        run: |
          echo "Deploying..."
        # In a real deploy, you'd capture output or status here

  notify_status:
    needs: deploy
    uses: ./.github/workflows/notify-status.yml
    with:
      deployment_status: ${{ needs.deploy.result }}
      repo: ${{ github.repository }}
      sha: ${{ github.sha }}
      recipient_email: "kturakannavar@deloitte.com"
